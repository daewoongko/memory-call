# 개발·테스트용. 실행에는 requirements.txt 만 있으면 된다.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   python -m pytest tests/ -q

-r requirements.txt

pytest>=8.0.0
httpx>=0.27.0          # fastapi.testclient 가 쓴다
